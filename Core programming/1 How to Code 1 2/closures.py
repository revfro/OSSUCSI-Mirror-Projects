import unittest

class Image:
	def __init__(self,a,b,c,d):
		self.width = a
		self.height = b
		self.fill = c
		self.colour = d













# Testing

class FnTest(unittest.TestCase):
    #
    def testBaseCase(self):
        self.assertEqual(fn(base), expected)
    def testAValue(self):
        self.assertEqual(fn(val1), expected)
    def testA2ndValue(self):
        self.assertEqual(fn(val2), expected)
    #

if __name__ == '__main__':
	unittest.main()
